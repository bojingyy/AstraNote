1. [Resolved 2026-06-03] Secured notes title be shown when searched (should be encypted)
    Implemented: secure note search now uses `secureTitleAlias` only (user-provided alias, default "Locked Note"). Real secured note titles are never used for search.

2. notifications being shown in the background layer when pop up window is opened

3. [Resolved] change text size, bold etc. in note workspace

4. [Resolved] can click subject text or empty space right after it to trigger the fold & unfold

5. 

6. [Resolved] add images as attachment

7. export as PDF

8. [Resolved] add last edit time for each note (inside note workspace)